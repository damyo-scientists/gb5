package com.game.gb5.deck.utils;

import com.game.gb5.character.model.CharacterStatus;
import com.game.gb5.character.model.GameCharacter;
import com.game.gb5.deck.model.Deck;

public class DeckMaker {
    public Deck makeDeck() {
        Deck deck = new Deck();
        GameCharacter gameCharacter = new GameCharacter();

        // ah,  so many fields in here, but we have to make maker right?
        CharacterStatus characterStatus = new CharacterStatus();
        return null;
    }
}
